settings:
  legacy:
    force: true
    interval: 0s
schema:
- name: work
  path: .
  commands:
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
